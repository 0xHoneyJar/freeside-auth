{
  "name": "@freeside-identities/engine",
  "version": "0.1.0",
  "private": true,
  "description": "Headless identity logic — 4-tier resolve algorithm, credential-link semantics, JWT helpers (verify side, not issuer), supersession-aware identity merge. Pure functions, no I/O. Adapters wrap; engine reasons.",
  "license": "MIT",
  "dependencies": {
    "@freeside-identities/protocol": "workspace:*",
    "@freeside-identities/ports": "workspace:*"
  }
}
