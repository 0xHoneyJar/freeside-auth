{
  "name": "@freeside-identities/ports",
  "version": "0.1.0",
  "private": true,
  "description": "TS interface ports — IIdentityService (resolve, register, link), IJwksProvider (verify, fetch JWKS), ICredentialBridge (translate external proof → canonical CredentialProof). Consumers depend on these; adapters satisfy them.",
  "license": "MIT",
  "dependencies": {
    "@freeside-identities/protocol": "workspace:*"
  }
}
